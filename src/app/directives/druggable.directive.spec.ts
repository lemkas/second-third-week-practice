import { DruggableDirective } from './druggable.directive';

describe('DruggableDirective', () => {
  it('should create an instance', () => {
    const directive = new DruggableDirective();
    expect(directive).toBeTruthy();
  });
});
